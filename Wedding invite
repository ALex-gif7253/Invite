<!DOCTYPE html>
<html lang="en">
<head>
<style>
/* style.css */

/* Basic layout */
body {
  font-family: Arial, sans-serif;
  margin: 0;
  padding: 0;
  background-color: #f7f7f7;
  display: flex;
  justify-content: center;
  align-items: center;
  flex-direction: column;
  min-height: 100vh;
}

h1 {
  color: #333;
}

.invitation {
  text-align: center;
  padding: 20px;
  background-color: #fff;
  border-radius: 8px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

.rsvp-container {
  margin-top: 30px;
  text-align: center;
}

button {
  padding: 10px 20px;
  font-size: 16px;
  background-color: #28a745;
  color: white;
  border: none;
  border-radius: 5px;
  cursor: pointer;
}

button:hover {
  background-color: #218838;
}

/* Puzzle piece styles */
#puzzle-container {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 20px;
}

.puzzle-piece {
  width: 100px;
  height: 100px;
  background-color: #ffcc00;
  border: 10px solid #ff9900;
  position: absolute;
  transition: all 1s ease-in-out;
}

#puzzle-piece1 {
  top: 20%;
  left: 20%;
}

#puzzle-piece2 {
  top: 20%;
  left: 30%;
}

/* After the pieces join */
.joined #puzzle-piece1 {
  transform: translate(40%, 0%);
}

.joined #puzzle-piece2 {
  transform: translate(-40%, 0%);
}
</style>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Wedding Invitation</title>
</head>
<body>
  <div id="puzzle-container">
    <div id="puzzle-piece1" class="puzzle-piece"></div>
    <div id="puzzle-piece2" class="puzzle-piece"></div>
  </div>

  <div class="invitation">
    <h1>You're Invited to Our Wedding!</h1>
    <p>Join us as we celebrate the love and union of [Your Name] & [Partner's Name].</p>
    <p>We can’t wait to share this special moment with you!</p>
  </div>

  <div class="rsvp-container">
    <h2>RSVP</h2>
    <form id="rsvp-form">
      <label for="name">Your Name:</label>
      <input type="text" id="name" required>
      <label for="guests">Number of Guests:</label>
      <input type="number" id="guests" required>
      <button type="submit" id="rsvp-button">Confirm RSVP</button>
    </form>
    <div id="rsvp-message"></div>
  </div>

  <script>
// script.js

// Wait for the page to load completely
window.addEventListener('load', () => {
  // Animation to join the puzzle pieces
  setTimeout(() => {
    document.getElementById('puzzle-container').classList.add('joined');
  }, 1000);
});

// Handle the RSVP form submission
document.getElementById('rsvp-form').addEventListener('submit', function (e) {
  e.preventDefault();
  
  const name = document.getElementById('name').value;
  const guests = document.getElementById('guests').value;
  
  // Simulate saving the RSVP data (here, just logging to console)
  console.log(`RSVP from: ${name}, Number of guests: ${guests}`);
  
  // Display confirmation message
  document.getElementById('rsvp-message').textContent = `Thank you, ${name}! Your RSVP is confirmed for ${guests} guests.`;
  
  // Optionally clear form fields after submission
  document.getElementById('rsvp-form').reset();
});

</script>
</body>
</html>
